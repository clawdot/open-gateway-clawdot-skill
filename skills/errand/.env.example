# ── 唯一必需注入：API_KEY（agent 身份，clw_，须已开通 errand 能力）────────────
# 缺 API_KEY 时脚本返回 RECOVERY[API_KEY_MISSING]，引导用户去注册页（CLAWDOT_SETUP_URL）
# 拿 key 后由 agent 写入本文件。GATEWAY_MCP_URL 没配也一并写（默认跑腿网关 MCP 端点）。
GATEWAY_MCP_URL=https://paotui.hicaspian.com/mcp/v1
API_KEY=your-errand-capable-api-key

# ── 用户授权 consent_grant（cg_，= 一个用户）：正常流程不用配置本节 ────────────
# 模型：一个 api_key 可服务多个用户，一个 cg 表示一个用户。开局只需注入 API_KEY；
# cg 由用户本人走一次【短信验证码】授权后，写入【能力分格】共享凭证缓存
# ~/.clawdot/errand-credentials.json（按 API_KEY+手机号键控，同一 API_KEY 下跑腿绑定
# 复用、skill 升级重装不丢），之后单用户业务命令无需 --phone。
#
#   首跑任意业务命令 → RECOVERY[USER_NOT_BOUND_NEEDS_SMS] →
#     request_user_bind --phone <11位> → verify_user_bind --phone <11位> --bind-id --code
#
# 【能力分格】跑腿授权与外卖独立、consent 分能力发放互不通用：同一手机号要用两条线得各绑
# 一次。跑腿 cg 写 errand-credentials.json、外卖 cg 写 credentials.json，各存各的、不互踢。
#
# CONSENT_GRANT_ID 是【只读预注入】项：仅无状态部署直接喂长效 cg 跳过绑定时才需要；
# CLI 不会回写本文件。多用户场景各自绑定、业务调用带 --phone 指定。
# 注意：open-gateway 无 admin 静默绑定——每个用户必须本人授权一次。
# CONSENT_GRANT_ID=

# ── 可选 ────────────────────────────────────────────────────────────────────
# 共享凭证缓存目录（默认 ~/.clawdot；容器/多租户部署可重定向）
# CLAWDOT_HOME=/path/to/home

# API_KEY 缺失时引导用户注册/登录的页面地址（默认 ClawDot developer 登录页）
# CLAWDOT_SETUP_URL=https://clawdot.hicaspian.com/developer/login
